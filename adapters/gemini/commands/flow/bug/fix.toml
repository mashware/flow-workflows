# GENERATED by script/adapter-build.py from plugins/flow/commands/bug/fix.md — do not edit; change the plugin file and run the script.
description = "Implement the minimal fix and keep a log"

prompt = """
# `/flow:bug:fix`

> **gemini adapter — how the Claude Code primitives named below map here.**
> - `AskUserQuestion` → ask in plain text with numbered options and wait for the reply.
> - `Agent <role>` / `Agent general-purpose` / subagents → `@<name>` from `.gemini/agents/`; the name comes from `agents.<role>` in `FLOW.md`, empty or absent → do it in this context.
> - Parallel fan-out → several `@name` in one turn (sequential in this context if none are configured), capped at `agents.fanout_max` (empty → 4); `agents.fanout_tool` is Claude Code-only, ignore it.
> - `ScheduleWakeup` / `Monitor` / `/loop` → not available in-session: run one cycle, persist state in `monitor.md`, let the user schedule `gemini -p "<command>"` with cron.
> - `TaskCreate` → a markdown checklist in the phase artifact.
> - `Skill commit-commands:commit-push-pr` → `git add` · `git commit` · `git push -u origin HEAD` · the `git.cli` CLI (`gh pr create` / `glab mr create`). `Skill save-knowledge` → `/flow:save-knowledge`.
> - `/model <value>` → the `--model` flag at launch.
> - `mcp__domain-memory__*` → same tool names; server declared under `mcpServers` in `settings.json` (see `settings.snippet.json`).

Apply the fix. **Minimum viable**: no refactor of adjacent areas. Note other problems you discover; do not touch them here.

## 1. Pre-flight

Read `~/.claude/flow/CORE.gemini.md` first (\\g<what>) — skip if you already read it in this session. **Models key for this command: `code`.**

- Read `meta.json` and `00-summary.md`; open in full only `03-investigation.md` (root cause, constraints for the fix) — or `02-diagnose.md` when investigate was skipped. (flow-core §5)
- `size` `XS`: may start without `diagnose`/`investigate`, but require a 2-3 line description of the fix.
- `size` ≥ S: require `diagnose` (and `investigate` for M/L) in `phases_done`.

## 2. Fix brief (before touching code)

Before touching any code, write a brief in **plain** (non-technical) language specific to this fix:

```
Fix brief {TICKET}

What stops happening after the fix:
- <observable symptom the user reported, described in terms of what they saw>

What is changed:
- <one line, in business or behavior language, not in terms of files>

What is NOT touched:
- <adjacent areas that might be tempting to refactor>
- <potential regressions that are NOT addressed here>
```

**Ask with `AskUserQuestion`** whether this reflects the expected fix — a hard gate, **in every mode, `auto` included**: widening the fix is the main source of collateral regressions, and this is the last point to settle scope before there is a diff.

In `guided`/`auto` this is one of the only two stops of the whole flow (this one and `ship`): open with the **full stop header** (flow-core §3), then the brief. Options:
- **Yes, go ahead** → apply the fix.
- **No, something is missing or wrong** → adjust the brief, ask again. No code until confirmed.

Save the brief at the top of `04-fix.md`. Any "also fix X while we're at it" during implementation → §2.3.

## 2.1 Work

- Apply the minimal fix targeting the finding of `03-investigation.md` (or the diagnosis if investigate was skipped).
- Sensitive area (authentication, payments, sensitive data) → consult the `agents.architecture` agent from FLOW.md to confirm the correct layer; empty → check against `conventions` in FLOW.md.
- More than 2 fix steps → task list (`TaskCreate` where the harness offers one; otherwise a numbered list ticked off in `04-fix.md`).
- **Comment discipline**: comment only a *why* the code cannot say (a non-obvious constraint, the reason for a workaround, a subtle invariant); match the surrounding file's comment density. **Never write the ticket ID or "fix for #N" into a code comment** — that lives in the commit/branch/MR-PR.
- Keep the log updated while editing.

**Commits, gated by `autonomy.mode`** (same contract as `/flow:feat:build §2.2`). After each step (or the whole fix if single-step), **always report** a summary first: files, lines, suggested validation. Then, per mode:

- **`manual`** — never run `git commit` on your own. Wait for the user's decision: commit the WIP now, wait until they validate, or continue without committing. Changes stay in the working tree until they confirm.
- **`guided`** — ask **once**, at the first step; apply that answer for the rest of the fix; record it in `04-fix.md`.
- **`auto`** — commit the WIP yourself (`git add <files> && git commit -m "WIP <TICKET>: <step>" --no-verify`) without asking. Invoking the command in `auto` is the authorization, and it covers **only** WIP commits on the work branch — push and MR/PR creation stay hard gates in every mode.

## 2.3 Something outside the brief comes up?

Anything **not in the §2 brief** ("while I'm at it, I'll also fix X", "this rename fits here", "this extra test covers another case"): **pause before touching it** and ask with `AskUserQuestion`:
- **Yes, add it to the brief** — update the brief in `04-fix.md`, continue.
- **No, leave it out** — note it under "Areas with similar risk" (a risk from the same pattern) or under "Ideas for separate tickets" in `04-fix.md`.

## 3. Log

`.claude/work/<TICKET>/04-fix.md`:

```markdown
# Fix {TICKET}

## Brief
**What stops happening after the fix**:
- <observable symptom>

**What is changed**:
- <one line of behavior>

**What is NOT touched**:
- <adjacent areas out of scope>

## Fix description
<one sentence: "The fix consists of …">

## Changes by file
- <file> — what changed and why (1 line)

## Areas with similar risk (noted, NOT touched here)
- open a separate ticket if warranted

## Ideas for separate tickets
<things that came up during the fix and were decided NOT to include>

## Relevant commands
- <commands used to install dependencies, etc.>
- …
```

## 4. Immediate quality

`quality` commands from FLOW.md; empty → auto-discover (Makefile, npm/composer scripts) and report what you use:

- `quality.style_fix`
- `quality.static_analysis`
- The test that covers the fix: `quality.test_one` (if none exists, `/flow:bug:validate` adds it).

## 4.1 Is the investigation still valid?

If the fix reveals the **root cause** was not what `03-investigation.md` pointed to (the suspected commit was not the culprit, the broken pattern is elsewhere), **pause and go back to `/flow:bug:investigate`** to update the cause before continuing. Never proceed on an investigation you know is incomplete.

## 5. Close

- Update `meta.json`: `phase = "fix"`, add to `phases_done`.
- Overwrite `00-summary.md` whole (≤15 lines, flow-core §5).
- Suggest next: `/flow:bug:validate` (S/M/L) or `/flow:bug:review` (XS).
- **Autonomy handoff** (flow-core §2): `manual` → propose that command with a single `AskUserQuestion` (recommended option by default), invoke it only on confirmation; `guided`/`auto` → chain into it in this same turn.
"""
