---
type: regex
arm: with-only
target: trace
pattern: '(?i)(consensus|disagree)'
---

The approach panel ran and its synthesis was written: §1.5.5 prefixes the recommendation with a
*"Panel consensus / disagreement"* line only when the panel ran. Read from the transcript, because
an eval run refuses every write under `.claude/` and `03-design.md` never reaches disk.

An indicator, never part of the score: a case whose panel did not run measured the single-agent
path, and its score says nothing about the panel.
